<local:BaseView x:Class="FSBeheer.View.TestPDFView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FSBeheer.View"
        mc:Ignorable="d"
        x:Name="PDF_Generate"
        DataContext="{Binding TestPDF, Source={StaticResource Locator}}"
        Title="TestPDFView" Height="323.832" Width="591.355">
    <StackPanel Orientation="Horizontal">
        <StackPanel Margin="5" Orientation="Vertical">
            <StackPanel Height="120"></StackPanel>
            <DockPanel Name="spInterfaceButtons" HorizontalAlignment="Right" Margin="2">
                <Button Content="Create" CommandParameter="{Binding ElementName=PDF_Generate}" Command="{Binding CreatePDFCommand}" Template="{DynamicResource InterfaceButtonTemplate}"></Button>
                <Button Content="Annuleer" CommandParameter="{Binding ElementName=PDF_Generate}" Command="{Binding CloseCommand}" Template="{DynamicResource InterfaceButtonTemplate}"></Button>
            </DockPanel>
        </StackPanel>
    </StackPanel>
</local:BaseView>
