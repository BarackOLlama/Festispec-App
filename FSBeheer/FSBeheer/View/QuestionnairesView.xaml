<Window x:Class="FSBeheer.View.QuestionnairesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FSBeheer.View"
        mc:Ignorable="d"
        Title="Questionnaires View" Height="400" Width="400"
        DataContext="{Binding QuestionnairesViewModel, Source={StaticResource Locator}}">
    <StackPanel>
        <Label>Questionnaires view</Label>
        <DataGrid ItemsSource="{Binding Questionnaires}" IsReadOnly="True" SelectedItem="{Binding SelectedQuestionnaire}"></DataGrid>
        <Button Command="{Binding ViewQuestionnaireCommand}">View Questionnaire</Button>
    </StackPanel>
</Window>
