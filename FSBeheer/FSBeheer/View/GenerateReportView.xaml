<local:BaseView x:Class="FSBeheer.View.GenerateReportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FSBeheer.View"
        mc:Ignorable="d"
        Title="Rapport genereren" Height="450" Width="800"
        DataContext="{Binding GenerateReport, Source={StaticResource Locator}}">
    <StackPanel Margin="20">
        <StackPanel Margin="30">
            <WrapPanel>
                <Label Content="Titel: "/>
                <TextBox Text="{Binding Inspection.Name, Mode=TwoWay}" Width="350"/>
            </WrapPanel>
        </StackPanel>
        <StackPanel Margin="10">
            <DataGrid MaxHeight="250" VerticalScrollBarVisibility="Auto" ItemsSource="{Binding Questionnaire}" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Inspectienummer" Binding="{Binding Id}" Width="200"></DataGridTextColumn>
                    <DataGridCheckBoxColumn Header="Checkbox" Binding="{Binding }"></DataGridCheckBoxColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </StackPanel>
</local:BaseView>
