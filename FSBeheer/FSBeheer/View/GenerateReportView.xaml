<local:BaseView x:Class="FSBeheer.View.GenerateReportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FSBeheer.View"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Rapport genereren" Height="450" Width="1100"
        DataContext="{Binding GenerateReport, Source={StaticResource Locator}}">
    <Window.Resources>
        <local:TypeToDisabledConverter x:Key="DisabledConverter"></local:TypeToDisabledConverter>
    </Window.Resources>
    <StackPanel Margin="20">
        <StackPanel Margin="30">
            <WrapPanel>
                <Label Content="Bestandnaam: " Margin="40,0,0,0"/>
                <TextBox Text="{Binding Filename, Mode=TwoWay}" Width="729" VerticalContentAlignment="Center"/>
            </WrapPanel>
            <WrapPanel Margin="18,10,0,10">
                <Label Content="Titel: "/>
                <TextBox Text="{Binding Title, Mode=TwoWay}" VerticalContentAlignment="Center" Width="800"/>
            </WrapPanel>
            <WrapPanel>
                <Label Content="Omschrijving: "/>
                <TextBox Text="{Binding Description, Mode=TwoWay}" VerticalContentAlignment="Center" Width="772"/>
            </WrapPanel>
            <WrapPanel Margin="18,10,0,10">
                <Label Content="Advies: "/>
                <TextBox Text="{Binding Advice, Mode=TwoWay}" Width="788.5" VerticalContentAlignment="Center"/>
            </WrapPanel>
        </StackPanel>
        <StackPanel Margin="10">
            <DataGrid MaxHeight="250" VerticalScrollBarVisibility="Auto" ItemsSource="{Binding Questions}" AutoGenerateColumns="False" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Vraag" Binding="{Binding Content}" Width="355" IsReadOnly="True"></DataGridTextColumn>
                    <DataGridTextColumn Header="Vraagtype" Binding="{Binding Type.Name}" Width="150" IsReadOnly="True"></DataGridTextColumn>
                    <DataGridTextColumn Header="Beschrijving" Binding="{Binding Comments}" Width="*" IsReadOnly="True"></DataGridTextColumn>
                    <DataGridCheckBoxColumn Header="Niet tonen" Binding="{Binding Path=DoNotShow}"  Width="70" ></DataGridCheckBoxColumn>
                    <DataGridCheckBoxColumn Header="Staafdiagram" Binding="{Binding BarChart, UpdateSourceTrigger=PropertyChanged}" Width="80">
                        <DataGridCheckBoxColumn.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="IsEnabled" Value="{Binding Type.Name, Converter= {StaticResource DisabledConverter}}" />
                            </Style>
                        </DataGridCheckBoxColumn.CellStyle>
                    </DataGridCheckBoxColumn>
                    <DataGridCheckBoxColumn Header="Cirkeldiagram" Binding="{Binding PieChart, UpdateSourceTrigger=PropertyChanged}" Width="90">
                        <DataGridCheckBoxColumn.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="IsEnabled" Value="{Binding Type.Name, Converter= {StaticResource DisabledConverter}}" />
                            </Style>
                        </DataGridCheckBoxColumn.CellStyle>
                    </DataGridCheckBoxColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
        <StackPanel Name="spInterfaceButtons" VerticalAlignment="Bottom" HorizontalAlignment="Right"
                    Orientation="Horizontal" Margin="0,0,25,0">
            <Button Content="Generate Standard Report" Command="{Binding CreateStandardPDFCommand}" Template="{DynamicResource InterfaceButtonTemplate}"></Button>
            <Button Content="Generate Bar Chart" Command="{Binding CreateBarChartCommand}" Template="{DynamicResource InterfaceButtonTemplate}"></Button>
            <Button Content="Generate Pie Chart" Command="{Binding CreatePieChartCommand}" Template="{DynamicResource InterfaceButtonTemplate}"></Button>
        </StackPanel>
    </StackPanel>
</local:BaseView>
